[Unit]
Description=Gunicorn instance to serve CarsApp
After=network.target

[Service]
User=younes
Group=www-data
WorkingDirectory=/root/mlcars
Environment="PATH=/usr/bin"
ExecStart=/usr/bin/gunicorn --workers 3 --bind unix:mlcars.sock -m 007 wsgi:app

[Install]
WantedBy=multi-user.target